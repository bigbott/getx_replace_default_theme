name: getx_replace_default_theme
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flex_color_scheme: ^7.3.1 
  flex_seed_scheme: ^1.5.0
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.7.0
  get: 5.0.0-release-candidate-9
  random_color_scheme: ^0.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

get_cli:
    sub_folder: false  
